(("~/.files/.zetta.d/source/config/annotate.el" ((39 43 "foobar" "test" 0 nil) (266 463 "foobar" "  ;; `annotation-file` variable per directory (can be done via
  ;; dir-locals.el - note logseq doesn't sync the dir-locals or
  ;; annotations flie.  Need to explicitly set the .dir-locals on each" 1 nil)) "86a09159b31478877453d3c1e1eaaa8c"))