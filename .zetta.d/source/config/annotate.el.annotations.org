(("~/.files/.zetta.d/source/config/annotate.el" ((770 967 "foobar" "  ;; `annotation-file` variable per directory (can be done via
  ;; dir-locals.el - note logseq doesn't sync the dir-locals or
  ;; annotations flie.  Need to explicitly set the .dir-locals on each" 1 nil)) "d7e07dd2e3204d81ce6a478a11fbe9a4"))