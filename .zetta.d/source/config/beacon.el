(use-package beacon
  :config
  (setq beacon-color 0.1) ;; subtle, subtle, subtle
  (setq beacon-blink-when-window-scrolls nil) ;; distracting, so remove
  (setq beacon-blink-duration 0.3) ;; fast blink
  )
