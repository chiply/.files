// emacs droppings (autosave #f#, lockfiles .#f, backups f~)
*~
*#
.#*
// git timeline lives on the hub only
.git
// OS noise
(?d).DS_Store
(?d)Thumbs.db
// hywiki referent cache: derived per-machine state; each device rebuilds
// its own — syncing it just churns directory mtimes on every device
.hywiki.eld
