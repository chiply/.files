---
description: Convert a dev-agent-backlog design document to Ralph prd.json format
argument-hint: [path/to/design-doc.org]
allowed-tools: [Bash, Read, Write, Glob, Grep]
---

## Context

The user wants to convert a design document to Ralph's prd.json format for autonomous execution.

## Instructions

Read the skill at `${CLAUDE_PLUGIN_ROOT}/skills/design-to-prd/SKILL.md` and follow its instructions.

Design doc path: $ARGUMENTS
